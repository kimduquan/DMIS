Installation

Copy Benchmark.ashx to the website's directory.
Copy Benchmark.dll to the website's bin directory.
Copy to all instance if the tested site runs NLBS.

Testing

Configure the BenchmarkTool (i.e. edit the BenchmarkTool.exe.config).
Run the BenchmarkTool.exe (always only one instance).
Analyze logs after tool is finished.
